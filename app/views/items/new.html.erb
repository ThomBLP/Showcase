
<%= simple_form_for @item, url: category_items_url, html: { multipart: true } do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>
  <%= f.hidden_field :category_id %>
  <%= f.button :submit, "Créer" %>
<% end %>
